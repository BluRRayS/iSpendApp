@using iSpendWebApp.Models.Account

@{
    ViewData["Title"] = "Dashboard";
    List<AccountViewModel> accounts = ViewBag.Accounts;
}

<h2 class="text-center">Dashboard</h2>
<div class="container">
    <div class="row">
        <div class="col-md-6">
            <div class="well" style="min-height: 400px">
                <canvas id="CategoryChart">
                </canvas>
            </div>
        </div>
        <div class="col-md-6" style="min-height: 400px">
            <div class="well">
                <select class="select-list" id="AccountMenu">
                    @*@foreach (var item in accounts)
                    {
                        <option value="item.AccountId">item.AccountName</option>
                    }*@
                </select>
                <canvas id="AccountBalanceChart">
                </canvas>
            </div>
        </div>
        <div class="col-md-12" style="min-height: 600px">
            <div class="well">
                <canvas id="TotalBalanceChart">

                </canvas>
            </div>
        </div>
    </div>
</div>

@* AccountMenu *@
<script>
    $('#AccountMenu').change(ReloadChart())

    function ReloadChart(parameters) {
        
    }

</script>

@* Category Chart *@
<script>
   
    var categories = @Html.Raw(ViewBag.Categories);
    var categoriesCosts = @Html.Raw(ViewBag.CategoriesCosts);

    var data = {
        labels: ["Avg inc/exp per category"],
       
    };

    var ctx = $("#CategoryChart").get(0).getContext("2d");
    var myBarChart = new Chart(ctx, {
        type: "bar",
        data: data,
    });

    var dataSetValue = [];
    for (var j = 0; j < categories.length; j++) {
        dataSetValue[j] = 
        {
            label: categories[j],
            backgroundColor: getRandomColor(),
            borderColor: getRandomColor(),
            borderWidth: 1,
            data: [categoriesCosts[j]]
        }
        data.datasets.push(dataSetValue[j]);
        myBarChart.update();
    }    

    function getRandomColor() {
        var letters = '0123456789ABCDEF'.split('');
        var color = '#';
        for (var i = 0; i < 6; i++ ) {
            color += letters[Math.floor(Math.random() * 16)];
        }
        return color;
    }
</script>
 
@* Saving Chart *@
<script>
    var ctx = document.getElementById('AvgCategoryChart').getContext('2d');
    var myChart = new Chart(ctx, {
        type: 'line',
        data: {
            labels: @Html.Raw(ViewBag.Categories),
            datasets: [
                {
                    label: 'Account balance',
                    data: @Html.Raw(ViewBag.CategoriesCosts),
                    borderColor: ['rgba(54, 162, 235, 1)'],
                    backgroundColor: ['rgba(54, 162, 235, 0.2)'],
                    borderWidth: 1
                }
            ]
        },
        options: {
            
        }
        
    });
</script>

@* Account Chart *@
<script>
    var ctx = document.getElementById('AccountBalanceChart').getContext('2d');
    var myChart = new Chart(ctx, {
        type: 'line',
        data: {
            labels: @Html.Raw(ViewBag.TimeStamps),
            datasets: [
                {
                    label: 'Expenditure for each Category',
                    data: @Html.Raw(ViewBag.AccountBalance),
                    borderColor: 'rgba(153, 102, 255, 1)',
                    backgroundColor: 'rgba(153, 102, 255, 0.2)',
                    borderWidth: 1
                }

            ]

        },
        options: {
            
        }
    });
</script>